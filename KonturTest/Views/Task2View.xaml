<UserControl x:Class="KonturTest.Views.Task2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:KonturTest.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:Task2ViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input file -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="Входной файл:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding InputPath, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Margin="4,0"/>
            <Button Grid.Column="2"
                    Content="Обзор…"
                    Command="{Binding BrowseInputCommand}"
                    Padding="8,2"/>
        </Grid>

        <!-- Output file -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="Выходной файл:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Margin="4,0"/>
            <Button Grid.Column="2"
                    Content="Обзор…"
                    Command="{Binding BrowseOutputCommand}"
                    Padding="8,2"/>
        </Grid>

        <!-- Start + ProgressBar -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Button Content="Старт"
                    Command="{Binding StartCommand}"
                    Padding="16,4"
                    MinWidth="80"/>
            <Button Grid.Column="1"
                    Content="Отмена"
                    Command="{Binding CancelCommand}"
                    Padding="16,4"
                    MinWidth="80"
                    Margin="8,0,0,0"/>
            <ProgressBar Grid.Column="2"
                         Minimum="0" Maximum="1"
                         Value="{Binding Progress}"
                         Height="22"
                         Margin="8,0"/>
            <TextBlock Grid.Column="3"
                       Text="{Binding Progress, StringFormat={}{0:P0}}"
                       VerticalAlignment="Center"
                       TextAlignment="Right"/>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Foreground="{Binding StatusColor}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,4"/>
    </Grid>
</UserControl>
